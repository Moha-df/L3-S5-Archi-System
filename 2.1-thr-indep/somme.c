#include <errno.h>
#include <pthread.h>
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    long int n, p;
    long int somme;

    printf("%ld\n", somme);
    printf("(attendu : %ld)\n", n * p * (n * p + 1) / 2);

    exit(0);
}
